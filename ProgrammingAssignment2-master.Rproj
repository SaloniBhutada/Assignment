cacheSolve <- function(y, ...) {
  inv <- y$getInverse()
  if(!is.null(inv)){
    message("getting cached data")
    return(inv)
  }
  data <- y$get()
  inv <- solve(data)
  y$setInverse(inv)
  inv      
}
